import 'package:flutter/material.dart';

class DeLetterCreator extends StatelessWidget {
  const DeLetterCreator({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(home: Scaffold(),);
  }
}
